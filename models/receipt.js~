var mongoose = require('mongoose'),  
    Schema   = mongoose.Schema;

var receipSchema = new Schema({  
  name:    { type: String },
  duration:     { type: Number },
  difficulty:  { type: String },
 
});

module.exports = mongoose.model('Receip', receipSchema);  
